export const metadata = {
    title: 'Job',
    description: 'Your new job is here',
}

export default function JobLayout({ children }) {
    return (
        <section>
            {children}
        </section>
    )
}