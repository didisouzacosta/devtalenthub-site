export const metadata = {
    title: 'Cookie preferences',
    description: 'More about our cookie collect preferencies',
}

export default function CookiePreference({ children }) {
    return (
        <section>
            {children}
        </section>
    )
}