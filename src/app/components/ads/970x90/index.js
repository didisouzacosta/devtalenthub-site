'use client'

import styles from './970x90.module.css'

export default function Banner970x90({ className }) {
    return (
        <div className={`${styles.container} ${className}`}>
            970x90
        </div>
    )
}